import ComingSoon from '@/components/common/ComingSoon';

export const metadata = { title: 'Support' };

export default function Page() {
  return (
    <ComingSoon
      eyebrow="Support"
      title="Real help, around the clock."
      body="Our 24/7 support hub is being built. For now, every booking confirmation in this demo is instant and refundable."
    />
  );
}
